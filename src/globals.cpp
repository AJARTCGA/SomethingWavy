#include "stdafx.h"
#include "globals.h"

std::vector<worm*> wormVariationContainer;

///Holds the different regions
std::vector<region*> regionContainer;

bool WAVE_CLEAR = true;

float totalValue;

int coinPouch = 10000;

std::vector<int> wormPouch;

region* currentRegionPosition;

region* targetRegionPosition;

int currentRegion = 0;

//im so sorry but running out of time :(